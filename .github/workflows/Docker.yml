name: Docker

on:
  workflow_call:
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
     - name: Docker Setup Buildx
       uses: docker/setup-buildx-action@v3.3.0

     - name: Build Docker image
       run: docker build -t challange-pic-pay-api:latest -f ../DockerFile .



